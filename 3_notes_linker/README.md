# Markdown Notes Analyzer

Скрипт анализирует markdown-заметки, ищет похожие файлы и может автоматически добавлять краткие summary через LLM.

## Установка зависимостей

```bash
pip install openai

python main.py ./notes
# AI Git Commit Generator

Скрипт автоматически генерирует git commit message на основе staged changes через LLM.

## Установка зависимостей

```bash
pip install openai

git add .

python main.py